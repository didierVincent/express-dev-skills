<%- include('../partials/header') %>
  <body>
    <p>Behold, my (developing) dev skills:</p>
    
    <ul>
      <% skills.forEach(function(s) { %>
      <li>
        <a href="/skills/<%= s.id %>"> <%= s.skill %></a>
        
        <% }); %>
      </li>
    </ul>
    <h4>(Go back to <a href="/">Home</a>)</h4>
  </body>
</html>
